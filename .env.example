# CoreInventory — Environment Variables
# Copy to .env and fill in your values. Never commit .env to git.

# Flask secret key (change this to any long random string)
SECRET_KEY=change-me-to-something-random

# ── Gmail OTP ──────────────────────────────────────────────────
# Your Gmail address
SMTP_USER=krrishmevada34@gmail.com

# Gmail App Password (NOT your regular password)
# Get it: myaccount.google.com → Security → App passwords
# Paste exactly as Google shows it — spaces are stripped automatically
SMTP_PASSWORD=rpnryjomlimlteez

# Display name shown in the email "From" field (optional)
SMTP_FROM_NAME=CoreInventory
